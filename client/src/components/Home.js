import React from "react";

const Home = () => {
  return (
    <div>
      <div>HOME! I AM RICH</div>
      <button onClick={() => console.log("CLICKED")}>Click Me</button>
    </div>
  );
};

export default Home;
