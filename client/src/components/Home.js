import React from "react";

const Home = () => {
  return (
    <div>
      <div>HOME!</div>
      <button onClick={() => console.log("CLICKED")}>Click Me</button>
    </div>
  );
};

export default Home;
